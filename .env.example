# Leave API/WS empty when using Docker frontend (nginx proxies /api and /ws).
# For local Vite (`npm run dev`), use http://localhost:8000 and ws://localhost:8000.
VITE_API_BASE_URL=
VITE_WS_BASE_URL=
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxx
